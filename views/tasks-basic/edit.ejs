<%- include('../partials/user-shell-start', { title, currentUser, currentPath }) %>
<section class="page-header page-header-inline">
  <div>
    <p class="tag">Task Management</p>
    <h1>Edit Task</h1>
    <p class="muted">Update task details and keep your tracking information accurate.</p>
  </div>
  <a href="/tasks/<%= task._id %>" class="button secondary">Back to Details</a>
</section>

<section class="card">
  <form action="/tasks/<%= task._id %>?_method=PUT" method="POST" enctype="multipart/form-data" class="form-grid two-columns">
    <label>
      Task Title
      <input type="text" name="title" value="<%= task.title %>" required />
    </label>

    <label>
      Subject
      <input type="text" name="subject" value="<%= task.subject || '' %>" required />
    </label>

    <label>
      Deadline
      <input type="datetime-local" name="deadline" value="<%= new Date(task.deadline).toISOString().slice(0, 16) %>" required />
    </label>

    <label>
      Priority
      <select name="priority" required>
        <option value="High" <%= task.priority === 'High' ? 'selected' : '' %>>High</option>
        <option value="Medium" <%= task.priority === 'Medium' ? 'selected' : '' %>>Medium</option>
        <option value="Low" <%= task.priority === 'Low' ? 'selected' : '' %>>Low</option>
      </select>
    </label>

    <label>
      Status
      <select name="status" required>
        <option value="Pending" <%= !task.completed ? 'selected' : '' %>>Pending</option>
        <option value="Completed" <%= task.completed ? 'selected' : '' %>>Completed</option>
      </select>
    </label>

    <label class="full-width">
      Description
      <textarea name="description" rows="5"><%= task.description %></textarea>
    </label>

    <label class="full-width">
      Replace Attachment
      <input type="file" name="file" accept=".pdf,image/*" />
      <% if (task.file) { %>
        <span class="muted small-text">Current file: <a href="<%= task.file %>" target="_blank" rel="noreferrer">Open current attachment</a></span>
      <% } %>
    </label>

    <div class="full-width form-actions">
      <button type="submit" class="button">Save Changes</button>
      <a href="/tasks/<%= task._id %>" class="button secondary">Cancel</a>
    </div>
  </form>
</section>
<%- include('../partials/user-shell-end') %>
